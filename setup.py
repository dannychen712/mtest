from distutils.core import setup

setup(
    name='mersivetest',
    author='danny',
    install_requires=[
        "pytest",
        "requests",
        "selenium",
        "pytest-selenium",
        "pytest-html"
    ]
)
